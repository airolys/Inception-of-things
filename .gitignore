# Ignore all Vagrant folders recursively (local and temporary files)
.vagrant/

# Vagrant should not be used in Part 3 (P3)
# But at home, on Mac, need to setup a VM first to simulate 42 school environement/VM
p3/Vagrantfile

## Romain questions
questions.txt